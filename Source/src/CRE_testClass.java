import java.util.Scanner;

public class CRE_testClass {

	public static void main(String[] args) {
		Scanner in = new Scanner(System.in);
		
		/*System.out.println("Start Reading:");
		while(in.hasNext()) {
			String next = in.next();
			System.out.println(next);
	}*/
		
		System.out.println("CAN 12345 1 00000 Bus Tour1 201810272215");
		
		/*String line = "";
		String delimiters = ",";
		String[] tempRowArray;

		BufferedReader reader = new BufferedReader(new FileReader(csvFile));
		try {
			line = reader.readLine(); // Read first line of file

		} catch (IOException e) {
			// Auto-generated catch block
			e.printStackTrace();
		}

		String[] dimension = line.split(delimiters);

		// Initialize m and n using file data and create instance array
		this.m = Integer.parseInt(dimension[0]);
		this.n = Integer.parseInt(dimension[1]);
		matrixArray = new double[this.m][this.n];

		// Initialize loop counter variables
		int r = 0, c = 0;

		try {
			while ((line = reader.readLine()) != null) {
				// Read a line from the file, representing a row of the matrix,
				// and store it in the array tempRowArray separating elements
				// using a comma
				tempRowArray = line.split(delimiters);

				// Put the numbers into their columns
				for (c = 0; c < this.n; c++) {
					matrixArray[r][c] = Double.parseDouble(tempRowArray[c]);
				}
				// Moving onto the next line so increment row
				r++;
			}

		} catch (IOException e) {
			// Auto-generated catch block
			e.printStackTrace();
		}*/
	}

}
